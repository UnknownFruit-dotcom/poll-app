services:
  db:
    image: mysql:8.0
    container_name: poll-app-db-dev
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: poll_app
      MYSQL_USER: laravel
      MYSQL_PASSWORD: Pswd-(19AA57
    ports:
      - "3306:3306"
    volumes:
      - mysql_data_dev:/var/lib/mysql

volumes:
  mysql_data_dev: